export enum EventCategory {
    API_EXPLORER = "Api Explorer",
    MAP = "Map",
    MAP_GL = "Map GL",
    CALENDAR = "Calendar",
    ACADEMY = "Academy",
    TOOLS = "Tools",
    MATH = "Math",
    CATEGORY = "Category",
    CAT = "Cat",
    DOG = "Dog",
    OTHERS = "Others",
}

export enum Action {
    COPY = "COPY",
    DELETE = "DELETE",
}

export enum State {
    PENDING = "PENDING",
}

export enum Status {
    SUCCESS = "SUCCESS",
    FAILED = "FAILED",
    CANCELLED = "CANCELLED",
}
